# Future Edits Tracker

Items we want to build eventually but haven't prioritised yet.
Add context so it's easy to pick up later.

---

## Charts & Visualisation

- **Nifty 50 TRI benchmarking** — we currently benchmark against the plain Nifty 50 price index (`^NSEI`). SEBI-regulated funds use TRI (Total Return Index), which reinvests dividends and runs ~1–2% higher annually. Switch benchmark source to Nifty 50 TRI so the comparison is more honest. Yahoo Finance ticker: `^NSEI` doesn't carry TRI; may need NSE's own data feed or a separate source.

- **Trading fees visual** — fees (STT + DP charges) are deducted from cash on execution but not surfaced anywhere in the UI. Add a summary somewhere — either a stat card on the Summary tab or a column in the Trades tab — showing total fees paid to date so the drag on returns is visible.

---

## Ideas / Exploratory

*(nothing yet)*
